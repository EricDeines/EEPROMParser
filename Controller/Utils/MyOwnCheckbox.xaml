<UserControl x:Class="EEPROMParser.Controller.MyOwnCheckbox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:EEPROMParser.Controller"
             mc:Ignorable="d">
  <Grid Margin="5,5,5,5">

    <Grid.RowDefinitions>
      <RowDefinition/>
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <CheckBox Grid.Column="0" VerticalAlignment="Center"
              IsChecked="{Binding RelativeSource={RelativeSource FindAncestor,
                            AncestorType={x:Type local:MyOwnCheckbox}},
                            Path=IsChecked}"/>
    <TextBox Grid.Column="1" VerticalAlignment="Center" Margin="5,0,0,0" BorderThickness="0" 
             Text="{Binding RelativeSource={RelativeSource FindAncestor,
                            AncestorType={x:Type local:MyOwnCheckbox}},
                            Path=Text}"
             IsReadOnly="true"/>
  </Grid>
</UserControl>